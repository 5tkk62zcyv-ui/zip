# TaxiTaShare Development Guide

이 저장소의 모든 개발 작업은 `docs/prd.md`를 제품 요구사항의 최상위 기준으로 삼는다. 구현, 리뷰, 테스트, 데이터 모델 변경 시 관련 FR/TR ID를 먼저 식별하고 결과 보고에도 포함한다.

UI와 시각 표현은 `docs/design.md`를 톤앤매너의 최상위 기준으로 삼고,
`docs/design-transition-plan.md`의 TaxiTaShare 적용 원칙과 전환 순서를
따른다. 새 화면이나 컴포넌트를 만들 때 임의 색상·그림자·radius·버튼
문법을 추가하지 말고 기존 디자인 토큰과 공통 variant를 우선 사용한다.
기능 정확성, 접근성, 안전 및 금액·상태의 명확한 표현은 시각적 모사보다
우선한다.

## 프로젝트 목표

TaxiTaShare는 같은 방향으로 이동하는 대학생 2~4명이 택시를 함께 이용하고, 관리자 지급 가상 포인트로 예상 요금을 예치한 뒤 실제 요금을 정산하는 모바일 우선 MVP다.

핵심 흐름:

1. 필수 사용자 정보 입력
2. 모집 생성 또는 AI 기반 후보 추천
3. 참여 신청·승인과 모집 확정
4. 예상 요금 기준 포인트 예치
5. 집결·이동과 노쇼 처리
6. 실제 요금 확인·이의제기
7. 최종 정산과 원장 기록

실제 카드 결제, 사용자 직접 충전, 현금 인출은 MVP 범위가 아니다.

## 기준 문서와 우선순위

충돌이 있을 때 다음 순서를 적용한다.

1. 사용자의 현재 명시적 요청
2. `docs/prd.md`의 Must 요구사항과 TR 요구사항
3. 이 `AGENTS.md`
4. 저장소의 기존 구현 패턴
5. mock 데이터와 임시 UI 동작

PRD에 결정되지 않은 정책은 임의로 확정하지 않는다. 특히 취소 수수료, 노쇼 제재, 분쟁 판정, 지도 제공자, 요금 산정 방식, Neon 운영 리전·백업 정책은 열린 결정으로 보고한다.

## 기술 스택

- Next.js 16 App Router
- React 19
- TypeScript
- Tailwind CSS 4
- shadcn 및 Base UI
- Neon PostgreSQL
- 네이버 지도 API 또는 카카오맵 API
- Vercel 배포
- pnpm

현재 구현의 mock 데이터는 화면 개발용이다. 권한, 잔액, 정산, 상태 전이 또는 추천 근거를 mock이나 클라이언트 상태에 맡기지 않는다.

## 기본 개발 절차

기능 개발에는 `$deliver-prd-feature`를 기본 진입점으로 사용한다.

1. `docs/prd.md`에서 관련 FR/TR ID, 상태, MVP 범위와 열린 이슈를 찾는다.
2. 영향을 받는 기존 코드와 데이터 흐름을 읽는다.
3. 아래 라우팅 표에 따라 필요한 스킬과 에이전트만 선택한다.
4. 읽기 전용 검토 결과에서 불변식, 권한, 실패 모드와 수용 기준을 확정한다.
5. 가장 작은 일관된 기능 단위만 구현한다.
6. 타입, 린트, 빌드와 관련 회귀 시나리오를 검증한다.
7. 변경 파일, 충족한 FR/TR ID, 검증 결과와 열린 결정을 보고한다.

불필요한 병렬 작업이나 전 영역 리뷰를 수행하지 않는다. 여러 에이전트가 작업할 때는 파일 또는 책임 범위를 명확히 나누고 다른 에이전트의 변경을 되돌리지 않는다.

## 스킬 및 에이전트 라우팅

| 변경 영역 | 프로젝트 스킬 | 담당 에이전트 |
|---|---|---|
| 복합 기능·마일스톤 | `$deliver-prd-feature` | 필요한 역할만 조정 |
| 모집·참여 상태, 권한, 취소, 만료, 노쇼, 분쟁 | `$design-domain-workflow` | `domain_architect` |
| 관리자 지급, 예치, 환불, 추가 차감, 정산, 감사 로그 | `$secure-point-settlement` | `ledger_security_reviewer` |
| 장소 검색, 거리·시간·요금, 지도 제공자, AI 추천 | `$validate-map-recommendation` | `map_recommendation_reviewer` |
| 스키마, 마이그레이션, 트랜잭션, 연결, 백업·복구 | `$evolve-neon-database` | `neon_database_reliability_reviewer` |
| Next.js, React, UI, 서버 액션·라우트 구현 | `$implement-nextjs-feature` | `nextjs_neon_implementer` |
| 수용 기준, 접근성, 회귀, 출시 준비 | `$verify-mvp-release` | `product_quality_reviewer` |

읽기 전용 에이전트는 코드를 수정하지 않는다. 구현 에이전트는 요청된 파일과 기능만 변경하고 검토 결과를 구현 제약으로 반영한다.

## 공식 Vercel 플러그인

Vercel, Next.js, React, Neon 연동의 최신 지침은 설치된 공식 `vercel@openai-curated` 플러그인만 사용한다. 해당 스킬을 프로젝트로 복제하거나 별도로 중복 설치하지 않는다.

필요할 때만 관련 공식 스킬을 선택한다.

- Next.js App Router·캐시·서버 경계: `nextjs`
- React 성능·구성: `react-best-practices`
- UI 컴포넌트: `shadcn`
- 함수 런타임: `vercel-functions`
- Neon 연동: `vercel-storage`
- 환경 변수: `env-vars`
- 브라우저 검증: `verification`, `agent-browser-verify`
- 배포·CI/CD: `deployments-cicd`
- 로그·성능·운영: `observability`

Vercel 배포는 사용자가 명시적으로 요청한 경우에만 수행한다.

## 도메인 불변식

- 가입 필수 정보가 없는 사용자는 모집 생성과 참여를 할 수 없다.
- 모집 목표 및 확정 인원은 2~4명이다.
- `OPEN` 모집에만 신규 참여 신청과 승인을 허용한다.
- 모집 종료 후 확정 인원이 2명 이상일 때만 출발 가능하다.
- AI 추천은 참여나 확정을 자동 수행하지 않는다.
- 모집 상태와 참여자 상태는 함께 검증한다.
- 취소, 만료, 노쇼, 정산 대기와 완료 전이는 서버에서 권한과 선행 상태를 확인한다.
- 상태 전이와 금액 변경은 재시도와 동시 요청에서도 같은 결과를 보장해야 한다.

상태를 추가하거나 의미를 바꿀 때는 `$design-domain-workflow`로 전이표, 권한, 실패 행동과 수용 테스트를 먼저 정의한다.

## 포인트와 정산

- 포인트는 관리자만 지급하는 가상 단위다.
- 포인트 원장은 append-only로 취급하고 과거 거래를 수정하지 않는다.
- 모든 지급·예치·반환·추가 차감을 감사 가능한 원장으로 기록한다.
- 일반 잔액과 예치 잔액을 구분한다.
- 금액은 양의 정수 단위로 검증하고 반올림 정책은 PRD의 계산식을 따른다.
- 권한, 잔액, 상태와 금액 검증은 서버에서 수행한다.
- 정산 상태 변경과 원장 기록은 하나의 데이터베이스 트랜잭션으로 처리한다.
- 멱등성 키는 데이터베이스 유일성 제약으로 보강한다.
- 중복 요청, 동시 요청, 잔액 부족, 이중 환불·차감과 부분 실패를 테스트한다.

포인트 또는 정산 변경에는 `$secure-point-settlement`와 `$evolve-neon-database`를 함께 적용한다.

## 지도와 AI 추천

- 장소 검색, 지오코딩, 경로, 거리·시간과 요금 계산을 제공자 중립 인터페이스 뒤에 둔다.
- 좌표계, 단위, 반올림, 산정 시각과 오류 형식을 경계에서 정규화한다.
- 추천은 실제 후보 모집 데이터와 계산 결과만 근거로 사용한다.
- 후보 모집 ID, 거리, 예상 시간, 요금 근거, 산정 시각과 추천 사유를 추적 가능하게 한다.
- 자연어 모델은 목적지, 경로, 시간 또는 요금을 생성하지 않는다.
- 추천 문구는 결정적 계산 결과에서만 만든다.
- 타임아웃, 제한, 캐시 노후화, 경로 없음과 제공자 장애를 처리한다.

관련 변경에는 `$validate-map-recommendation`을 적용한다.

## Next.js 및 UI 원칙

- App Router와 Server Component를 기본으로 사용하고 필요한 곳에만 좁은 Client Component 경계를 둔다.
- 인증·권한·입력·금액·상태 검증을 서버 액션 또는 Route Handler에서 다시 수행한다.
- 기존 `components/`와 모바일 셸 패턴을 우선 재사용한다.
- 로딩, 빈 상태, 오류, 비활성, 성공과 재시도 상태를 명시한다.
- 모바일 한 손 조작, 키보드 탐색, 포커스 순서, 의미 있는 레이블을 보장한다.
- 상태는 색상만으로 전달하지 않는다.
- 관련 없는 파일, 디자인 시스템 또는 패키지를 변경하지 않는다.
- 실제 비밀값을 저장소에 기록하지 않는다.

## Neon 및 데이터 변경

- 명시적 기본 키, 외래 키, 유일성, 체크 제약과 감사 시각을 정의한다.
- 도메인 불변식을 가능한 범위에서 데이터베이스 제약으로 보강한다.
- 마이그레이션은 기존 데이터 검증, 적용 순서, 전진 호환성과 롤백 또는 보상 절차를 포함한다.
- 개발·프리뷰·운영 데이터와 비밀값을 분리한다.
- 서버리스 연결 풀링, 타임아웃, 연결 수명과 배포 리전을 고려한다.
- 백업 보유, 복구 훈련, 복구 목표와 운영 알림이 정해지지 않았으면 열린 결정으로 보고한다.

## 검증

변경 범위에 비례해 다음을 실행한다.

```powershell
pnpm lint
pnpm exec tsc --noEmit
pnpm build
```

추가로 관련된 경계값과 실패 시나리오를 검증한다.

- 2명 미만, 2명, 4명, 4명 초과
- 모집 수동 종료, 자동 종료, 취소와 만료
- 중복 참여·정산 요청과 동시 요청
- 잔액 부족, 노쇼, 실제 요금 이의제기와 기한 만료
- 지도 API 오류, 시간 초과와 경로 없음
- 로딩, 빈 상태, 네트워크 실패와 재시도

실행하지 않은 검사를 통과했다고 보고하지 않는다. 기존 실패와 새 변경으로 발생한 실패를 구분한다.

## 변경 완료 보고

최종 보고에는 다음을 포함한다.

- 구현 또는 검토 결과
- 변경한 파일
- 관련 FR/TR ID
- 사용한 프로젝트 스킬과 에이전트
- 사용한 공식 Vercel 스킬
- 실행한 검증과 결과
- 남은 위험, 미결 정책과 후속 작업

애플리케이션 코드를 변경하지 않은 설정·문서 작업에서도 변경 범위와 검증 여부를 명확히 보고한다.
